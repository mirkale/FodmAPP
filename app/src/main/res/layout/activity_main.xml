<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/searchButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="46dp"
        android:onClick="searchButtonPressed"
        android:text="HAKU"
        app:layout_constraintBottom_toTopOf="@+id/myListButton"
        app:layout_constraintStart_toStartOf="@+id/myListButton" />

    <Button
        android:id="@+id/myListButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="7dp"
        android:layout_marginBottom="42dp"
        android:onClick="myListButtonPressed"
        android:text="OMA"
        app:layout_constraintBottom_toTopOf="@+id/myDiaryButton"
        app:layout_constraintEnd_toEndOf="@+id/myDiaryButton" />

    <Button
        android:id="@+id/myDiaryButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="48dp"
        android:onClick="myDiaryButtonPressed"
        android:text="PÄIVÄKIRJA"
        app:layout_constraintBottom_toTopOf="@+id/guideButton"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/guideButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="7dp"
        android:layout_marginBottom="113dp"
        android:onClick="guideButtonPressed"
        android:text="OHJE"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/myDiaryButton" />
</android.support.constraint.ConstraintLayout>